# Docker Compose specific variables (optional)
COMPOSE_PROJECT_NAME=hrms_platform

# Global API Keys or settings (if any, usually service-specific)
# SENTRY_DSN="" # Example for a global Sentry DSN if not per-service
# COMMON_THIRD_PARTY_API_KEY=""

# Note: Most environment variables are service-specific and should be
# defined in the .env.example files within each service directory
# (e.g., services/auth/.env.example, services/hrms/.env.example)
# and for client apps (e.g., web-client/.env.example).
